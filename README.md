# 🤖 GigaChat_Proxy

Полнофункциональное веб-приложение для взаимодействия с GigaChat API (Сбербанка) через удобный интерфейс. Проект состоит из FastAPI бэкенда и React фронтенда с поддержкой потоковой передачи ответов в реальном времени.

[![Python](https://img.shields.io/badge/Python-3.8+-blue)](https://www.python.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green)](https://fastapi.tiangolo.com/)
[![React](https://img.shields.io/badge/React-18.2+-blue)](https://react.dev/)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

## 📋 Содержание

- [Возможности](#-возможности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [Использование](#-использование)
- [API Документация](#-api-документация)
- [Архитектура](#-архитектура)
- [Разработка](#-разработка)

## ✨ Возможности

- 🔐 **Аутентификация**: HTTP Basic Authentication для безопасного доступа
- 💬 **Многодиалоговый интерфейс**: Создание и управление несколькими диалогами
- ⚡ **Потоковая передача**: Real-time SSE для получения ответов по частям
- 📱 **Адаптивный дизайн**: Современный UI на React с KaTeX для математических формул
- 🔄 **Кэширование токенов**: Оптимизированное взаимодействие с GigaChat API
- 🎨 **Уведомления**: Система toast-уведомлений для информирования пользователя
- 🖥️ **Tauri поддержка**: Возможность компиляции как десктопное приложение
- ⚙️ **CORS включен**: Настроено для локальной разработки

## 📦 Требования

### Бэкенд
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Фронтенд
- Node.js 16+ и npm/yarn

### Прочее
- GigaChat API ключ (получить можно в [личном кабинете Сбербанка](https://gigachat.devices.sberbank.ru/))
- Интернет соединение

## 🚀 Установка

### Клонирование репозитория

```bash
git clone https://github.com/DarkMK69/GigaChat_Proxy.git
cd GigaChat_Proxy
```

### Установка бэкенда

```bash
cd backend

# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

### Установка фронтенда

```bash
cd frontend

# Установка зависимостей npm
npm install
```

## ⚙️ Конфигурация

### Переменные окружения (Бэкенд)

Создайте файл `.env` в директории `backend/` на основе `.env_example`:

```bash
cd backend
cp .env_example .env
```

Отредактируйте `.env` файл:

```dotenv
GIGACHAT_API_KEY=your_gigachat_api_key_here
```

**Получение API ключа:**
1. Перейдите на [сайт GigaChat](https://gigachat.devices.sberbank.ru/)
2. Авторизуйтесь или создайте аккаунт
3. Найдите раздел API ключей
4. Скопируйте ваш API ключ

### Аутентификация в приложении

По умолчанию используется HTTP Basic Authentication. Учетные данные можно изменить в файле `backend/app/auth.py`:

```python
VALID_CREDENTIALS = {
    "user": "password",
    "admin": "admin123"
}
```

## 🎯 Запуск

### Запуск бэкенда

```bash
cd backend

# Убедитесь, что виртуальное окружение активировано
python -m app.main

# или используйте uvicorn напрямую
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Бэкенд запустится на `http://localhost:8000`

**Проверка здоровья сервера:**
- Откройте браузер: `http://localhost:8000/docs` (Swagger UI)
- Попробуйте запрос с Basic Auth

### Запуск фронтенда

```bash
cd frontend

# Режим разработки
npm run dev

# Или для production сборки
npm run build
npm run preview
```

Фронтенд по умолчанию запустится на `http://localhost:5173`

## 💻 Использование

### Веб-приложение

1. **Откройте приложение**: Перейдите на `http://localhost:5173`

2. **Вход**: Используйте учетные данные для входа (по умолчанию: `user`/`password`)

3. **Создание диалога**: Нажмите кнопку "+ Новый диалог"

4. **Отправка сообщения**: 
   - Введите текст в поле ввода
   - Нажмите Enter или кнопку отправки
   - Ответ будет загружаться в реальном времени

5. **Множественные диалоги**: Переключайтесь между диалогами через боковую панель

### API примеры

#### Проверка доступа

```bash
curl -u user:password http://localhost:8000/
```

#### Отправка сообщения (обычный режим)

```bash
curl -X POST http://localhost:8000/request \
  -u user:password \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет! Как дела?", "stream": false}'
```

#### Потоковая передача ответа

```bash
curl -X POST http://localhost:8000/request \
  -u user:password \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!", "stream": true}'
```

## 📚 API Документация

### Автоматическая документация

После запуска бэкенда откройте:
- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

### Основные endpoints

#### GET `/`
Проверка аутентификации и здоровья сервера

**Параметры:**
- Basic Auth (обязательно)

**Ответ:**
```json
{
  "message": "Chat Proxy API",
  "user": "username"
}
```

#### POST `/request`
Отправка сообщения и получение ответа от GigaChat

**Body:**
```json
{
  "message": "Ваше сообщение",
  "dialog_id": "default",
  "stream": false
}
```

**Параметры:**
- `message` (string): Текст сообщения
- `dialog_id` (string, optional): ID диалога для контекста
- `stream` (boolean): Использовать потоковую передачу

**Ответ (stream=false):**
```json
{
  "response": "Ответ от GigaChat"
}
```

**Ответ (stream=true):**
Server-Sent Events формат:
```
data: {"chunk": "Текст", "accumulated": "Полный текст...", "done": false}
```

## 🏗️ Архитектура

```
GigaChat_Proxy/
├── backend/                    # FastAPI приложение
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py            # Главное приложение FastAPI
│   │   ├── auth.py            # Аутентификация
│   │   ├── models.py          # Pydantic модели
│   │   ├── giga_chat.py       # Прокси для GigaChat API
│   │   └── giga_chatmock.py   # Mock версия для тестирования
│   ├── requirements.txt        # Python зависимости
│   └── .env_example           # Пример переменных окружения
│
├── frontend/                   # React приложение
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   │   ├── Chat.jsx       # Основной чат интерфейс
│   │   │   ├── DialogList.jsx # Список диалогов
│   │   │   ├── Login.jsx      # Форма входа
│   │   │   ├── Message.jsx    # Компонент сообщения
│   │   │   └── ToastContainer.jsx  # Уведомления
│   │   ├── styles/            # CSS стили
│   │   ├── utils/             # Утилиты
│   │   │   ├── notifications.js # Система уведомлений
│   │   │   └── textProcessor.js # Обработка текста
│   │   ├── App.jsx            # Корневой компонент
│   │   └── main.jsx           # Точка входа
│   ├── package.json
│   └── index.html
│
└── README.md                   # Этот файл
```

### Стек технологий

**Бэкенд:**
- **FastAPI** - современный веб-фреймворк
- **Uvicorn** - ASGI сервер
- **httpx** - асинхронный HTTP клиент
- **python-dotenv** - управление переменными окружения
- **Pydantic** - валидация данных

**Фронтенд:**
- **React 18** - UI фреймворк
- **Vite** - сборщик и dev-сервер
- **KaTeX** - рендеринг математических формул
- **Tauri** - фреймворк для десктопных приложений

## 🔧 Разработка

### Структура фронтенд компонентов

#### Chat.jsx
Основной компонент чата, управляет:
- Отправкой сообщений
- Потоковой загрузкой ответов
- Отображением истории диалога
- Валидацией данных

#### DialogList.jsx
Список доступных диалогов:
- Переключение между диалогами
- Отображение названия и дат создания

#### Login.jsx
Форма аутентификации:
- Ввод учетных данных
- Валидация полей
- Отображение ошибок

### Разработка бэкенда

#### auth.py
Содержит функции аутентификации:
- `authenticate_user()` - проверка credentials
- Можно расширить интеграцией с БД

#### giga_chat.py
Класс GigaChatProxy для работы с API:
- `send_message()` - обычный запрос
- `send_message_stream()` - потоковая передача
- `_get_access_token()` - получение токена

#### Тестирование с mock

Используйте `giga_chatmock.py` для разработки без реального API:

```python
from app.giga_chatmock import GigaChatProxyMock

# Вместо реального прокси в main.py
# giga_chat = GigaChatProxy(api_key=os.getenv("GIGACHAT_API_KEY"))
giga_chat = GigaChatProxyMock()
```

### Hot Reload

Оба сервера поддерживают горячую перезагрузку:
- **Бэкенд**: `--reload` флаг в uvicorn
- **Фронтенд**: автоматически в Vite dev режиме

### Отладка

**Бэкенд:**
```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

**Фронтенд:**
- Откройте DevTools (F12)
- Вкладка Console для логов
- Вкладка Network для запросов

## 🐛 Решение проблем

### CORS ошибки
Убедитесь, что бэкенд запущен на `http://localhost:8000` или добавьте origin в `main.py`:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://your-domain.com"],
    ...
)
```

### Ошибка аутентификации
- Проверьте учетные данные в `auth.py`
- Убедитесь, что заголовок Authorization отправляется правильно

### GigaChat API недоступен
- Проверьте интернет соединение
- Убедитесь, что API ключ валиден
- Проверьте статус GigaChat сервиса

### SSL ошибки
В разработке SSL проверка отключена в `giga_chat.py`. В production используйте:

```python
ssl_context = ssl.create_default_context()
# Не отключайте check_hostname и verify_mode!
```

## 📝 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👨‍💻 Автор

**DarkMK69** - [GitHub профиль](https://github.com/DarkMK69)

## 🤝 Контрибьютинг

Приветствуются pull requests и issues!

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте [существующие issues](https://github.com/DarkMK69/GigaChat_Proxy/issues)
2. Создайте новый issue с описанием проблемы
3. Прикрепите логи ошибок и информацию о среде

## 🗺️ Дорожная карта

- [ ] Интеграция с базой данных для сохранения истории
- [ ] WebSocket поддержка для большей производительности
- [ ] Поддержка множественных пользователей с БД
- [ ] История сообщений с поиском
- [ ] Экспорт диалогов в PDF/TXT
- [ ] Десктопное приложение через Tauri
- [ ] Docker контейнеризация
- [ ] Unit и E2E тесты

---

**⭐ Если проект вам понравился, не забудьте поставить звезду!**